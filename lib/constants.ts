// Where the nav "Get The App" button points. No native app ships yet, so this
// is a coming-soon placeholder — swap in the App Store / Play links here when
// the app launches (single source of truth).
export const GET_THE_APP_URL = "/app";
